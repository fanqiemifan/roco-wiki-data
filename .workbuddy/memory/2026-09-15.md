# 2026-09-15

## final_forms.json 补充 form 字段
- 需求：为 `data/final_forms.json` 的每条记录补充 `form` 字段，数据源为 `data/pets.json`，按 `id` == `pet_id` 匹配。
- 实现：读取 pets.json 的 `items`，构建 `pet_id -> form` 映射后合并；`form` 键插入在 `name` 之后。
- 结果：259 条全部匹配成功（无未匹配、无 id 冲突），其中 form 有值 77 条（如 3012 鸭吉吉=蓬松的样子、3458 圣代甜甜=樱桃草莓口味），空值 182 条，按源数据原样写入空字符串 `""`（非 null）。
- 文件写回：`json.dump(..., ensure_ascii=False, indent=2)` + 结尾换行，保持原格式（2 空格缩进）。git diff: 259 insertions。

## 数据约定（本仓库）
- `data/final_forms.json`：字段顺序 id / name / form / leader_potential / main_type / sub_type / implemented；无子属性时 `sub_type` 用 `null`。
- `data/pets.json`：`{version, updatetime, source, generated_at, count, keep_fields, sort_by, items[]}`，item 主键 `pet_id`（唯一），含 `form`（形态，空串表示无形态）。
- `data/attribute_mapping.json`：属性编号 -> 属性名映射（如 06=光、18=恶）。
